import React from 'react';

const Loader = () => {
    return (
            <span uk-spinner="ratio: 4.5" className="uk-margin-small-top"  style={{display: 'flex', justifyContent: 'center',}}></span>
    );
};

export default Loader;